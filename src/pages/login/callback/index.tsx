import clsx from 'clsx'

export default function LoginCallbackPage() {
  return (
    <div
      className={clsx('flex', 'h-[100vh]', 'justify-center', 'items-center')}
    >
      <div>잠시만 기다려주세요</div>
    </div>
  )
}
